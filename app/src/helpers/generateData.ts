type data = {    
  mean?: number;
  stdv?: number;
}

const generateProbabilityData = (data: data) => {
  return 1
}

const generateCumulativeData = (data: data) => {
  return 1
}

export { generateCumulativeData, generateProbabilityData }